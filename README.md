# 壁の盤（firedash）

Fire TV（リモコン）と Echo Show 15（指・壁掛け）で使う大画面ダッシュボード。
決定事項は [要件.md](要件.md)（番号付き）。全体図は DS9i vault の
「システムマップ/プロジェクト/壁の盤 firedash.md」。

## どこで開くか

| どこ | 住所 |
|---|---|
| 道具棚から | 「壁の盤」の札（入口の下では `/ban/` へ行く） |
| 入口ごし | https://ds9.d-shino.workers.dev/ban/ （札が要る） |
| 直に | https://dshino9.github.io/firedash/ |
| 手元で直す間 | `preview_start` の firedash（:8797） |

`Bash` で直接 http.server を起こさない（作業場の決まりごと 3）。
更新は `git push` だけ（GitHub Pages が配る）。

## 操作

| したいこと | リモコン | 指 |
|---|---|---|
| 枠をえらぶ | 十字 | — |
| 窓の中に入る／出る | 決定 ／ 戻る | 枠を押す |
| 窓を替える | **W** | 枠の見出しの「替える ▾」 |
| 大きく（一時的に1枚） | **F**（戻るで解除） | — |
| かたちを変える | **1〜5** | 上の帯 |
| 設定 | 上の帯へ十字で上がって決定 | ⚙ 設定 |

## 窓の足しかた

`panels/` に1ファイル増やし、`app.js` の `PANELS` に足すだけ。窓は次の形。

```js
export default {
  id:'...', name:'...', icon:'🙂', hint:'窓えらびに出る一行',
  create(ctx){                    // ctx = { note, cfg, save }
    return {
      el,                         // 必須。枠の中に入る要素
      tick(){},                   // 任意。1秒ごと
      resize(){},                 // 任意。枠の大きさが変わったとき
      onKey(e){ return false; },  // 任意。true を返すと盤に渡さない
      enter(){}, leave(){},       // 任意。中に入った／出た
      refresh(){}                 // 任意。設定が変わった
    };
  }
};
```

## 守ること

- **`?.` と `??` を書かない。** Fire OS の WebView は版が古いことがある
- 端末に残す控えは `firedash-` 接頭辞だけ（Cache Storage はオリジン共有）
- 焦点の移動は「帯が重なっているものを優先」。中心の距離だけで測ると右を押して上へ飛ぶ
- 遠くから見る画面。焦点の枠は常に見えること

## いまの状態（2026-09-12）

- 盤（型・割り当て・焦点・設定）…… 動く
- 時計と天気 …… 動く（Open-Meteo を直に引く）
- 耳読の処理状況 …… 動く（入口の控えごし。Mac 直は APK にしてから）
- カレンダー／YouTube …… **仮の中身**。繋ぎ込みはこれから（要件 5・6）
- APK の器 …… 未着手（要件 2：Web を仕上げてから）
- 公開 …… 済み。道具棚に載せ、入口 ds9 の下に `/ban/` を開けた（札が要る）
