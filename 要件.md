# 壁の盤（Fire TV / Echo Show 15）— 要件

Fire TV（リモコン）と Echo Show 15（指・壁掛け）で同じ画面を使う。
中身は Web で作り、固まってから APK（WebView 1枚）で包む。

決めたこと（2026-09-12 本人と1問ずつ）

| # | 項目 | 決めたこと | 状態 |
|---|---|---|---|
| 1 | 機種 | Fire TV と Echo Show 15 の両方。入力の種類を見て焦点の枠を出し分ける | 着手 |
| 2 | 作り | Web で作る。Fire では APK（WebView）として動かす。**Web を仕上げてから包む** | Web を公開済み・APK は未着手 |
| 3 | 載せる窓 | 時計と天気・カレンダー・YouTube・耳読の処理状況 の4つ | 着手 |
| 4 | 分割 | 1〜4 の型を選び、そのあと枠ごとに窓を選ぶ。**起動中いつでも変更可** | 動く |
| 5 | カレンダー | Google カレンダー。**複数カレンダーを選べる** | 月表示・週表示は動く（中身は仮） |
| 6 | YouTube | 枠の中で再生。弾かれたら公式アプリへ渡す | **BGM の棚**として実装（本人の指定） |
| 7 | 耳読 | 家の中は Mac 直（:8770）、届かなければ Cloudflare の控え | 繋いだ（直は APK から） |
| 8 | 天気 | Open-Meteo（鍵不要）。座標は設定に持つ | 着手 |

## 5 の見せ方（2026-09-12 本人の指定）

**本当の壁掛けカレンダーのような月表示。それと週表示。**
祝日は holidays-jp（鍵不要）。日曜と祝日は赤、土曜は青、今日は枠ごと明るく。
配り口 `/ban/calendar.json` が決まれば、描く所は直さずに中身だけ入れ替わる。

## 5 の支度（ご本人・ブラウザで10分）

1. Google Cloud でプロジェクトを作る
2. **Google Calendar API** を有効化
3. OAuth 同意画面 → 外部 → テスト → テストユーザーに自分を入れる
4. 認証情報 → OAuth クライアント ID → **デスクトップ アプリ**（ウェブアプリではない。
   戻り先の登録が要らないので早い）
5. ターミナルで `python3 firedash/tools/google-consent.py`
   → 同意を通す → カレンダーを選ぶ → 入口の Secret に預ける（ここまで自動）
6. 画面に出る通りに入口を配り直す

入口の側はもう出来ている（`GET /ban/calendar.json`。Secret が無ければ 503 で
「まだ Google に繋いでいません」と言う）。盤の窓もその形をそのまま描く。

## 5 の実装方針（端末でログインさせない）

Fire TV のリモコンで Google のログインは通さない。**同意は Mac のブラウザで1回だけ**通し、
`refresh_token` を Cloudflare の金庫（Secret／KV）に入れる。Worker が入場券を取り直し、
選んだカレンダーを束ねて1つの JSON で配る。端末はその JSON を読むだけ。
複数カレンダーの選択は `calendarList` から取った一覧を端末の外（KV）に控える。
→ ds9-gate の原則「ログインさえすれば、どこでも自分の環境が出る」に沿う。

## 7 でぶつかった壁（2026-09-12・実測）

**https の盤から http の Mac は読めない。** ブラウザが混在内容として塞ぐ。設定では回避できない。
そこで常の道を入れ替えた：**Mac が入口（ds9）の KV に要約を置き、盤は札の内側で読む。**
盤は先に Mac 直を試す作りにしてあるので、**APK（WebView）で包んだ時点で「家の中は直」が効く**
（WebView は混在内容を許せる）。要件 2 の APK は、この一点でも先送りできない。

- Mac 側：`serve.py` の `/api/ban`（要約）と `watch_ban()`（置きにいく見張り）
- 入口側：`POST /__gate/ban`（鍵で確かめる・札は不要）と `GET /ban/status.json`（札が要る）
- **上げる回数は絞る。** 入口の KV は1日1000回で止まり、過去に使い切った事がある。
  「変わった時だけ・作業中は5分・暇なら30分」。控えは1時間で消える

## 決めていないこと

- ~~YouTube の一覧の出どころ~~ → **BGM のプレイリストを初期値に**（2026-09-12 本人）。
  新着の一覧ではなく「押せば流れ続ける棚」。正本は `playlists.json`。
  登録チャンネルの新着が欲しくなったら、5 の同意に `youtube.readonly` を足す。
- 盤の並び（型と割り当て）を端末に覚えさせるか、KV に上げて端末間で揃えるか。
  いまは端末の中（`firedash-v1`）。上げる余地を残した形にしてある。

## 端末側の制約（作るときに守る）

- Fire OS の WebView は Chromium だが版が古いことがある。**`?.` と `??` は使わない。**
- TV は縁が切れる（オーバースキャン）。設定の「TV の縁」で内側に寄せられる。
- 遠くから見る画面。文字は大きく、焦点の枠は常に見えること。
